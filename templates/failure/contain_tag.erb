<% if actual.element.nil? %>
Expected <%= actual.name %> to contain <%= expected.inspect %> but the element did not exist.
<% else %>
Expected <<%= actual.name %>> to contain <<%= expected %> <%= @options %>> but it did not.
<% end %>
